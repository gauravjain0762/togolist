import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const GuideRequest = () => {
  return (
    <View>
      <Text>GuideRequest</Text>
    </View>
  );
};

export default GuideRequest;

const styles = StyleSheet.create({});
